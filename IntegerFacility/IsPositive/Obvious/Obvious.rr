realization Obvious
  implements IsPositive for IntegerFacility

  function IsPositive (restores i: Integer) : control
    variable z: Integer
    IsPositive := IsGreater(i, z)
  end IsPositive

end Obvious
